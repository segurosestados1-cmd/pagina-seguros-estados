# Inicio Rápido - Seguros del Estado

## ⚠️ IMPORTANTE: Instalar Node.js Primero

Antes de continuar, necesitas instalar Node.js. Lee el archivo `INSTALACION_NODEJS.md` para las instrucciones completas.

## Pasos Rápidos (Después de Instalar Node.js)

### 1. Abrir Terminal en el Proyecto
```powershell
cd "C:\Users\usuario\Documents\aplicacion\seguros del estado\seguros del estado"
```

### 2. Instalar Dependencias
```powershell
npm install
```
Esto instalará todas las dependencias necesarias (Next.js, React, TypeScript, etc.)

### 3. Ejecutar el Proyecto en Modo Desarrollo
```powershell
npm run dev
```

### 4. Abrir en el Navegador
Abre tu navegador y ve a: **http://localhost:3000**

## Comandos Disponibles

- `npm run dev` - Inicia el servidor de desarrollo
- `npm run build` - Crea una versión optimizada para producción
- `npm start` - Ejecuta la versión de producción (después de build)
- `npm run lint` - Verifica errores en el código

## Estructura del Proyecto

```
seguros del estado/
├── app/              # Páginas y rutas
├── components/       # Componentes reutilizables
├── public/          # Archivos estáticos
└── package.json     # Configuración del proyecto
```

## Próximos Pasos

1. ✅ Instalar Node.js (ver INSTALACION_NODEJS.md)
2. ✅ Instalar dependencias (`npm install`)
3. ✅ Ejecutar el proyecto (`npm run dev`)
4. 🔄 Personalizar el diseño
5. 🔄 Agregar imágenes y contenido
6. 🔄 Conectar formularios con backend

## ¿Problemas?

- Si `npm` no se reconoce: Instala Node.js (ver INSTALACION_NODEJS.md)
- Si hay errores de instalación: Verifica que Node.js esté correctamente instalado
- Si el puerto 3000 está ocupado: Next.js te sugerirá usar otro puerto automáticamente

